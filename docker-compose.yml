networks:
  cms-network:
    driver: bridge
  elastic-network:
    driver: bridge

volumes:
  data_opencart-3:
  data_fluentbit:
  data_opensearch:

include:
  - ./prod/docker-cms-compose.yml
  - ./prod/docker-opensearch-compose.yml
